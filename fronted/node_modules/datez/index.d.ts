import { DateTime } from 'luxon';
export default class Datez {
    static fromFormat(baseDate: string, format: string, data: {
        zone: string;
    }): DateTime;
    static fromDate(baseDate: Date, data: {
        zone: string;
    }): DateTime;
    static fromISO(baseDate: string, data: {
        zone: string;
    }): DateTime;
    static setZone(date: DateTime, zone: string): DateTime;
}
